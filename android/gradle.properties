org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# org.gradle.java.home was removed because it contained a Windows path
# If you need to set a custom Java home, set it to a valid Linux path, e.g. /usr/lib/jvm/java-21-openjdk-amd64
# org.gradle.java.home=/path/to/java
# FlutterFire plugins still require the compatibility path until they migrate.
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
